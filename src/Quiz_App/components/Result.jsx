// Result.js
import React from "react";

const Result = ({ score }) => (
  <div className="results">
    <h2>Your Score: {score}</h2>
    {/* Additional components or messages for completed quiz */}
  </div>
);

export default Result;
